.row
  %aside.span4
    %section
      %h1= @group.name
      %p
        Organiser: 
        = @group.user.name
      - if !@group.limit.blank?
        %p
          Limit: 
          = h(@group.limit)
      - else
        %p No limit set please contact the organiser
  .span8
    %h3 Participants